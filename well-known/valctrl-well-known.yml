generated: '2026-07-21'
method: searched
source: https://valctrl.com
host: https://valctrl.com
note: valctrl.com is a single-page stealth landing site (React SPA behind Cloudflare) that returns HTTP
  200 with the SPA HTML shell for every path, so a 200 status alone does not indicate a real document.
  Content-type was checked for each probe; only robots.txt and sitemap.xml are genuine documents. No /.well-known/
  discovery surface exists. robots.txt carries Cloudflare-managed Content Signals (search=yes, ai-train=no,
  use=reference) plus AI-crawler disallows, saved verbatim as valctrl-robots.txt.
hosts:
- host: https://valctrl.com
  documents:
  - path: /.well-known/security.txt
    status: 200
    content_type: text/html
    note: SPA fallback page, not a security.txt
  - path: /.well-known/openid-configuration
    status: 200
    content_type: text/html
    note: SPA fallback page, not an OIDC discovery document
  - path: /.well-known/oauth-authorization-server
    status: 200
    content_type: text/html
    note: SPA fallback page, not an RFC 8414 document
  - path: /.well-known/api-catalog
    status: 200
    content_type: text/html
    note: SPA fallback page, not an API catalog
  - path: /.well-known/ai-plugin.json
    status: 200
    content_type: text/html
    note: SPA fallback page, not an ai-plugin manifest
x-shape-fix:
  converted: '2026-08-20'
  from: documents
  note: Rewritten into hosts[] -> documents[], the only shape well_known_docs() in score.rb reads. A served
    .well-known surface recorded in any other shape scores as absent.
